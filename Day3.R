#
#3일차
#
# 0. Open
# 1. Review

# &와 &&, |와 ||의 차이
vt <- c( TRUE, FALSE )
vt2 <- c( TRUE, FALSE )

vt & vt2   #벡터 요소 각각에 대하여 AND 연산 수행
vt && vt2   #벡터 첫번째 요소에 대하여 AND 연산 수행

# 2. Topic
#   2.1 list
#   2.2 factor
#   2.3 matrix
#   2.4 data frame
#   2.5 
